{
  "$schema": "http://json.schemastore.org/proxies",
  "proxies": {
    "WellKnownProxy": {
      "matchCondition": {
        "route": "/.well-known/{filename}"
      },
      "backendUri": "http://%WEBSITE_HOSTNAME%/api/WellKnown/{filename}"
    },
    "Domain Redirect": {
      "matchCondition": {
        "route": "/{shortUrl}"
      },
      "backendUri": "http://localhost/api/UrlRedirect/{shortUrl}"
    },
    "Root": {
      "matchCondition": {
        "route": "/"
      },
      "responseOverrides": {
        "response.statusCode": "301",
        "response.statusReason": "Moved Permanently",
        "response.headers.location": "%defaultRedirectUrl%"
      }
    }
  }
}